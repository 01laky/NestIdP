# UI screenshots

PNG captures of the NestIdP admin console and SAML login page (Evergreen UI). Used in the [operator tutorial](../tutorial.md) and root [README](../../README.md).

| File                                       | Screen                                                                  |
| ------------------------------------------ | ----------------------------------------------------------------------- |
| `admin-login.png`                          | Operator login (`/admin/login`)                                         |
| `admin-dashboard.png`                      | Dashboard — counts, sync status, IdP URLs                               |
| `idp-settings-overview.png`                | IdP settings — metadata/SSO URLs, entity ID                             |
| `idp-settings-certificate-metadata.png`    | IdP settings — signing cert, metadata preview                           |
| `idp-settings-signing-and-encryption.png`  | IdP settings — signing (active) + encryption panel overview             |
| `idp-settings-encryption-cert-options.png` | IdP settings — encryption cert generate options (RSA/EC, key transport) |
| `idp-settings-upload-certificate.png`      | IdP settings — upload PEM cert and key                                  |
| `api-connection-edit.png`                  | Edit API connection                                                     |
| `api-connection-sync.png`                  | API connection sync page                                                |
| `identity-users-list.png`                  | Users list (paginated)                                                  |
| `identity-groups-list.png`                 | Groups list                                                             |
| `identity-roles-list.png`                  | Roles list                                                              |
| `sp-connection-new-grafana.png`            | New SP connection (Grafana Cloud example)                               |
| `sp-connections-list.png`                  | SP connections list                                                     |
| `saml-login.png`                           | End-user SAML login (`/login`)                                          |

Regenerate by replacing PNGs in this folder; keep kebab-case names stable so links in docs do not break.

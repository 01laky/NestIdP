# Local Cursor config and rules — not shared via git
.cursor/

# Local prompts — not shared via git
prompts/

# Local mock identity API for manual sync testing — remove when done
mock-app/

# Dependencies and build output
node_modules/
dist/
build/
coverage/
apps/web/test-results/
apps/web/playwright-report/

# Environment
.env
.env.local
.env.*.local
.env.docker

# IDE and OS
.idea/
.vscode/
.DS_Store
*.log

# Prisma
apps/api/prisma/*.db
apps/api/prisma/*.db-journal
apps/api/prisma/.test-migration-sync.lock
apps/api/data/
*.tsbuildinfo

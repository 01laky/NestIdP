# Local AI assistant config — not shared via git
.cursor/
CLAUDE.md

# Local prompts — not shared via git
prompts/

# Local mock identity API for manual sync testing — remove when done
mock-app/

# Dependencies and build output
node_modules/
dist/
build/
coverage/
apps/web/test-results/
apps/web/playwright-report/

# Environment
.env
.env.local
.env.*.local
deploy/.env.docker.prod
deploy/.env.docker.dev
.env.docker

# IDE and OS
.idea/
.vscode/
.DS_Store
*.log

# Prisma
apps/api/prisma/*.db
apps/api/prisma/*.db-journal
apps/api/prisma/*.db-wal
apps/api/prisma/*.db-shm
apps/api/prisma/.scratch.db*
apps/api/prisma/.test-migration-sync.lock
apps/api/data/
# Local DB backup/dump artifacts (db-cli.mjs)
*.dump.sql
*.tsbuildinfo
.claude/

# Local SAML SP test harness — local only, not committed
sp-app/
